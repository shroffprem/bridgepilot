{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "phone": {
      "type": "string",
      "description": "Mobile number \u2014 used to match with TeamMember directory for scoped access"
    }
  },
  "required": [
    "role"
  ]
}